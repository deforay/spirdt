# ─── Application ───
APP_NAME="SPI-RDT Assessment Platform"
APP_ENV=local              # local | staging | production
APP_DEBUG=true             # MUST be false in production
APP_URL=http://localhost:8080
APP_TZ=UTC

# ─── Multi-tenancy ───
# Controls UI ONLY — whether organisation management is visible.
# It never gates the data model or tenant scoping. Single-tenant
# installs still carry organization_id on every row.
MULTI_TENANT=false

# ─── Database ───
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=spirdt
DB_USER=spirdt
DB_PASS=spirdt-local-dev

# ─── Auth ───
# Generate with: php -r "echo bin2hex(random_bytes(32));"
JWT_SECRET=442b9185a25e042679ba8a3fdf8d22a9c7ae751e3e1cb58e80c763b4a6463a21
JWT_ACCESS_TTL=900              # 15 minutes
# Long refresh TTL is deliberate: an auditor may be offline for days between
# issuing a token and syncing. Token expiry must never destroy local drafts.
JWT_REFRESH_TTL=2592000         # 30 days

# ─── CORS ───
# Comma-separated origins for the auditor PWA and admin builds.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# ─── Uploads ───
# Signatures and photos. Keep in step with docker/php/php.ini and the
# nginx client_max_body_size, or large media fails at the edge.
UPLOAD_MAX_BYTES=10485760       # 10 MB per file
STORAGE_PATH=/var/www/html/var/uploads

# ─── Logging ───
LOG_LEVEL=debug                 # debug | info | notice | warning | error
LOG_RETENTION_DAYS=30

# Host port for MySQL. 3307 is taken on some machines by another
# Docker project — override rather than stopping the other stack.
DB_EXPOSED_PORT=3308

# Names the CLI that bin/dev/review invokes. Local only.
REVIEW_AGENT=
REVIEW_AGENT_ARGS=
