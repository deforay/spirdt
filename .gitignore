/vendor/
/node_modules/
/var/log/*
/var/cache/*
/var/exports/*
/var/backups/*
/var/uploads/*
!/var/**/.gitkeep
.env
.php-cs-fixer.cache
.phpunit.result.cache
phpstan-baseline.neon
.DS_Store
/public/build/

# mkdocs build output
/site/

# The two source instrument documents are tracked, in resources/source/, so the
# extracted text and the generated template can be checked against what they came
# from. A Word file anywhere else is somebody's working copy and stays out.
*.docx
*.doc
!resources/source/*.docx
