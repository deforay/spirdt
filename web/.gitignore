node_modules/

# There is no dist/ here. The app builds into ../public, which is the document
# root, and the output is tracked — the machines it is served from do not all
# have node on them. See .gitattributes and docs/deployment.md.
dist-ssr/
*.tsbuildinfo
.vite/
*.local
