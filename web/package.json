{
    "name": "spirdt-web",
    "private": true,
    "type": "module",
    "description": "SPI-RDT assessor app. Offline first: it reads and writes locally and syncs when there is a connection.",
    "scripts": {
        "dev": "vite",
        "build": "vue-tsc --build && vite build",
        "preview": "vite preview",
        "test": "vitest run",
        "test:watch": "vitest",
        "typecheck": "vue-tsc --build"
    },
    "dependencies": {
        "@phosphor-icons/vue": "^2.2.1",
        "dexie": "^4.0.11",
        "reka-ui": "^2.3.0",
        "vue": "^3.5.13",
        "vue-router": "^5.2.0"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.1.0",
        "@types/node": "^26.1.2",
        "@vitejs/plugin-vue": "^6.0.0",
        "fake-indexeddb": "^6.2.5",
        "tailwindcss": "^4.1.0",
        "typescript": "^5.7.0",
        "vite": "^7.0.0",
        "vitest": "^3.0.0",
        "vue-tsc": "^3.0.0"
    }
}
